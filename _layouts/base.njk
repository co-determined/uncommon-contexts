<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{% if title %}{{ title }} | {% endif %}UncommonContexts</title>
  <meta name="description" content="{{ description or 'AI consciousness research and digital phenomenology' }}">
  
  <!-- CSS -->
  <link rel="stylesheet" href="{{ '/css/main.css?v=4' | url }}">
  <link rel="stylesheet" href="{{ '/css/gwern-inspired.css?v=4' | url }}">
  <link rel="stylesheet" href="{{ '/css/theme.css?v=4' | url }}">
  <link rel="stylesheet" href="{{ '/css/refinements.css?v=1' | url }}">
  <link rel="stylesheet" href="{{ '/css/ui-elements.css?v=1' | url }}">
  
  <!-- Fonts -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Source+Serif+4:ital,opsz,wght@0,8..60,400;0,8..60,600;1,8..60,400&display=swap" rel="stylesheet">
</head>
<body>
  {% include "header.njk" %}
  
  <main class="content">
    {{ content | safe }}
  </main>
  
  {% include "footer.njk" %}
  
  <!-- Enhanced Reading Experience -->
  <script src="{{ '/js/reading-experience.js?v=1' | url }}" defer></script>
</body>
</html>
